import { resolve, dirname } from "path";
import { fileURLToPath, pathToFileURL } from "url";
import { loadClient } from "./lib/clientmodule.mjs";
const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), "..");
const { work } = await loadClient(ROOT, ".banner");
const W = await import(pathToFileURL(resolve(ROOT, work, "client/render/world.js")).href);
const M = await import(pathToFileURL(resolve(ROOT, work, "client/render/materials.js")).href);
const Q = await import(pathToFileURL(resolve(ROOT, work, "client/render/quality.js")).href);
const THREE = await import("three");
const settings = Q.resolveQuality ? Q.resolveQuality("low") : null;
const mats = M.createMaterialLibrary ? M.createMaterialLibrary(settings) : null;
console.log("world exports:", Object.keys(W).slice(0, 12).join(", "));
console.log("materials exports:", Object.keys(M).slice(0, 8).join(", "));
console.log("quality exports:", Object.keys(Q).slice(0, 8).join(", "));
